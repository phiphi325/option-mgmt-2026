# Phase 0 placeholder Dockerfile for the Next.js 16.2.6 web app.
# Replaced in M0.4 with the real image (multi-stage with corepack/pnpm,
# next build, runtime via node:22-alpine, port 3000).
#
# This stub exists so `docker compose build web` does not fail in M0.1.
FROM node:22-alpine
WORKDIR /code
RUN echo "Next.js 16.2.6 shell arrives in M0.4 — see plan §17 / §19 day 4" > /tmp/M0.1_PLACEHOLDER
CMD ["sh", "-c", "echo 'apps/web: M0.1 placeholder; replaced in M0.4'; sleep 86400"]
