# Learning Suggestions — M1.16a Thread Export

**Captured:** 2026-05-13
**Thread:** M1.16a: POST /engine/collar-builder
**Source:** Hyperagent `/learning` page

---

## Summary

| Category | Count |
|---|---|
| **Thread-specific suggestions** | 196 |
| **Total skill suggestions** | 51 |
| **Total memory suggestions** | 348 |
| **Total agent suggestions** | 23 |

These suggestions were generated by Hyperagent's learning engine from this thread's conversation.
They are **pending** — not yet accepted or committed to the knowledge base.

---

## Why These Are Not Fully Documented Here

The learning suggestions are held in a **review queue** accessible only through the Hyperagent UI
at `/learning`. The `SearchKnowledge` SDK tool only surfaces *accepted* knowledge items; pending
suggestions are not queryable via any available API or tool call.

As a result, the 196 thread-specific suggestions (and the 51 skills / 348 memories / 23 agents
from the broader queue) cannot be enumerated programmatically from this export session.

---

## What the Suggestions Represent

Based on the conversation content of this thread, the pending suggestions likely include:

### Memory Suggestions (~348 total, 196 thread-specific)

The memory suggestions capture facts and patterns observed during the M1.16a development session:

- **Project state facts** — engine version `1.5.0`, test count ~840, 18 API endpoints live
- **Workflow patterns** — ruff I001 aliased-import fix, ProblemDetails RFC 7807 `"title"` vs `"detail"` field
- **Architecture decisions** — `§22.11 H5` underlying_qty from DB only, `_SUPPORTED_TICKERS` Phase 1 scope
- **CI patterns** — 5 CI jobs, guard sequence: ticker → IV history ≥ 30 → underlying_qty ≥ 100
- **Technical debt notes** — CHANGELOG `[1.5.0]` entry missing for M1.11a
- **Next-milestone context** — M1.11b collar integration, engine version `1.6.0` target

### Skill Suggestions (~51 total)

Skills extracted from techniques demonstrated in conversation, potentially including:

- Patterns for thin API service layers (guard → DB read → engine call → response)
- Pydantic schema patterns (`extra="forbid"`, `from_engine()` classmethods)
- FastAPI `ValueError → HTTPException(422)` routing patterns
- GitHub multi-file commit via `GITHUB_COMMIT_MULTIPLE_FILES` (upserts, not files)

### Agent Suggestions (~23 total)

Agent design proposals derived from observed workflows, potentially including:

- Specialized sub-agents for CI gate checking, engine pure-function validation, etc.

---

## How to Review and Accept Suggestions

1. Navigate to **Learning** in the Hyperagent sidebar (`/learning`)
2. Filter by "This Thread" to see the 196 thread-specific suggestions
3. Review each suggestion — accept to add to the knowledge base, dismiss to discard
4. Accepted memories are immediately available via `SearchKnowledge`
5. Accepted skills become available for future agent threads

---

## Recommendation

For the **M1.11b thread**, before starting work, visit `/learning` and accept the
high-value memory suggestions from this thread. In particular, look for:

- The M1.16a handoff memory (active_work category, importance 0.95) — should already be
  accepted if you confirmed the card presented at thread close
- Any memory about ProblemDetails RFC 7807 `"title"` field behavior
- Any memory about ruff I001 aliased-import ordering rules
- Any memory about the CHANGELOG `[1.5.0]` debt

Accepting these before M1.11b starts will give the agent better context from the outset.
